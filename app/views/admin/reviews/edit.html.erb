<% content_for :page_title, "Edit Review" %>
<% content_for :page_description, "Update review from #{@review.reviewer_name}" %>

<div class="mb-5">
  <%= link_to admin_reviews_path, class: "inline-flex items-center gap-1 text-[12px] text-zinc-400 hover:text-zinc-600 transition" do %>
    <i data-lucide="arrow-left" class="w-3.5 h-3.5"></i>Back to Reviews
  <% end %>
</div>

<%= render 'form', review: @review %>
