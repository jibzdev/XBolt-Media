<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Your Password - XBolt</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: #ffffff;
      color: #333333;
      line-height: 1.6;
    }
    .email-wrapper {
      width: 100%;
      background: #ffffff;
      padding: 0;
      margin: 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background: #ffffff;
    }
    .header {
      background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
      padding: 60px 40px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    .logo {
      width: 200px;
      height: auto;
      max-width: 100%;
      position: relative;
      z-index: 1;
    }
    .hero-section {
      background: #facc15;
      padding: 50px 40px;
      text-align: center;
      color: #000000;
    }
    .greeting {
      font-size: 32px;
      color: #000000;
      margin: 0 0 20px;
      font-weight: 800;
    }
    .hero-message {
      font-size: 18px;
      margin: 0 0 40px;
      max-width: 450px;
      margin-left: auto;
      margin-right: auto;
      font-weight: 500;
      line-height: 1.5;
    }
    .cta-button {
      display: inline-block;
      background: #000000;
      color: #ffffff;
      text-decoration: none;
      padding: 18px 40px;
      border-radius: 8px;
      font-weight: 700;
      font-size: 16px;
      margin: 0;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    }
    .cta-button:hover {
      background: #1a1a1a;
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
    }
    .content {
      padding: 60px 40px;
      background: #ffffff;
    }
    .section-title {
      font-size: 24px;
      color: #1a1a1a;
      margin: 0 0 30px;
      font-weight: 700;
      text-align: center;
    }
    .security-notice {
      background: #f8fafc;
      padding: 40px;
      margin: 40px 0;
      border-radius: 12px;
      border: 1px solid #e2e8f0;
      text-align: center;
    }
    .security-title {
      font-size: 20px;
      color: #1a1a1a;
      margin: 0 0 20px;
      font-weight: 700;
    }
    .security-text {
      color: #4a5568;
      font-size: 16px;
      margin: 0 0 20px;
      line-height: 1.6;
    }
    .expiry-notice {
      color: #71717a;
      font-size: 14px;
      margin: 20px 0 0;
      font-style: italic;
    }
    .footer {
      background: #1a1a1a;
      padding: 40px;
      text-align: center;
    }
    .footer-text {
      color: #a1a1aa;
      font-size: 14px;
      margin: 0 0 16px;
    }
    .contact-info {
      color: #ffffff;
      font-size: 16px;
      margin: 0;
      font-weight: 500;
    }
    .contact-link {
      color: #facc15;
      text-decoration: none;
      font-weight: 600;
    }
    .contact-link:hover {
      text-decoration: underline;
    }
    
    @media (max-width: 600px) {
      .header {
        padding: 40px 20px;
      }
      .hero-section {
        padding: 40px 20px;
      }
      .content {
        padding: 40px 20px;
      }
      .footer {
        padding: 30px 20px;
      }
      .greeting {
        font-size: 26px;
      }
      .logo {
        width: 160px;
      }
      .security-notice {
        padding: 30px 20px;
        margin: 30px 0;
      }
      .cta-button {
        padding: 16px 32px;
        font-size: 15px;
      }
    }
  </style>
</head>
<body>
  <div class="email-wrapper">
    <div class="container">
      <div class="header">
        <img src="<%= absolute_url(brand_favicon_url) %>" alt="Logo" class="logo">
      </div>
      
      <div class="hero-section">
        <h2 class="greeting">Password Reset Request</h2>
        <p class="hero-message">
          We received a request to reset your password. Click the button below to create a new password for your account.
        </p>
        <a href="<%= edit_reset_password_url(token: @user.reset_password_token) %>" class="cta-button">Reset Password</a>
      </div>
      
      <div class="content">
        <h3 class="section-title">Security Information</h3>
        
        <div class="security-notice">
          <h4 class="security-title">Important Security Notice</h4>
          <p class="security-text">
            This password reset link is valid for 10 minutes from the time it was sent. If you didn't request this password reset, please ignore this email and your password will remain unchanged.
          </p>
          <p class="expiry-notice">
            For your security, this link will expire automatically to protect your account.
          </p>
        </div>
      </div>
      
      <div class="footer">
        <p class="footer-text">© <%= Time.current.year %> XBolt. All rights reserved.</p>
        <p class="contact-info">
          Questions? Reach out to us at <a href="mailto:rkcustomsportsmouth@gmail.com" class="contact-link">rkcustomsportsmouth@gmail.com</a>
        </p>
      </div>
    </div>
  </div>
</body>
</html>